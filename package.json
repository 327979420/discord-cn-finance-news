{
  "name": "discord-cn-finance-news",
  "version": "0.1.0",
  "private": true,
  "description": "抓取金融与突发新闻，压缩为中文短讯并推送到 Discord。",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "node --test",
    "check": "node scripts/check.js && node --test",
    "test:webhook": "node src/cli-test-webhook.js",
    "run:once": "RUN_ONCE=true node src/index.js"
  }
}
